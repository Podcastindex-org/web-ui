# Podcastindex-org/web-ui — README Codespaces docs (follow-up to #586)

## Status
Draft only — not pushed or opened. Branch: `docs/codespaces-readme`.

## Plain-English description
Now that the Dev Container config from #586 (closing #553) is merged, the README
doesn't mention it yet. This adds a Codespaces badge near the top and a short
"Codespaces / Dev Containers" subsection under Getting Started, so contributors
opening the repo see the option before working through the manual `.env` setup
steps. Kept intentionally short to match the README's existing terse,
code-block-driven style — one paragraph, one code block, done.

## Proposed PR

**Title:** docs: mention Dev Container / Codespaces setup in README

**Body:**
```
Follow-up to #586 (closes #553): adds an "Open in GitHub Codespaces" badge
near the top of the README and a short Codespaces / Dev Containers
subsection under Getting Started, so contributors know the option exists
before working through the manual .env setup steps.
```
